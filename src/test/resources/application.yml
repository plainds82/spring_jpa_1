spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernates:
##        show_sql: true
#        format_sql: true

# 스프링부트에서 설정값이 없으면 자동으로 메모리 모드로 동작함.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace